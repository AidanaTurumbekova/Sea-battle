print("Welcome to Battleship!")
player_name = input("Enter your name: ")
print(f"Hello, {player_name}! Let's start!")

battlefield = [["~"] * 7 for _ in range(7)]

for row in battlefield:
    print(" ".join(row))
